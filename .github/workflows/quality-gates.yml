name: Quality Gates

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/python-quality-gates.yml@live-defi-rollout
    with:
      dep_repos: "unified-api-contracts unified-internal-contracts unified-events-interface unified-trading-library market-data-processing-service"
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
